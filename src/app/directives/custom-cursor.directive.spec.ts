import { CustomCursorDirective } from './custom-cursor.directive';

describe('CustomCursorDirective', () => {
  it('should create an instance', () => {
    const directive = new CustomCursorDirective();
    expect(directive).toBeTruthy();
  });
});
